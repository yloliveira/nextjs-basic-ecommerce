import { Product } from "@/app/models/product";
import { Model } from "miragejs";
import { ModelDefinition } from "miragejs/-types";

export const ProductModel: ModelDefinition<Product> = Model.extend({});
